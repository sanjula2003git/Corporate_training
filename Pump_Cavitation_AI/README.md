# Pump Cavitation AI

A beginner-friendly Streamlit project showing how mechanical engineers can combine pump sound, vibration, pressure and flow measurements to distinguish cavitation from other faults and decide what to inspect first.

## Run

```powershell
python -m pip install -r requirements.txt
python -m streamlit run app.py
```

The dataset is simulated for teaching. The app is not a machinery safety controller.
